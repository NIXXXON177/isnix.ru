# config/tab/groups.yml — эталон для ISNIX (TAB 5.5 + LuckPerms + isnix-opac-tab)
# После заливки: /tab reload
#
# ВАЖНО: placeholder только %isnix:clan_tag% (не %synced:...%)
# Суффикс клана — справа от ника; префикс админа — слева (LuckPerms)

_DEFAULT_:
  tabprefix: "%luckperms:prefix%"
  tabsuffix: "%luckperms:suffix% %isnix:clan_tag%"
  tagprefix: "%luckperms:prefix%"
  tagsuffix: "%luckperms:suffix% %isnix:clan_tag%"
