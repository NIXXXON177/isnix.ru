# ISNIX GraveGuard — защита при луте могилы

Fabric **1.21.1**, серверный мод `isnix-graveguard`.

Исправляет техсбой **Vanilla Tweaks Graves v4** (ly-graves): при PvP у могилы вещи пропадают. Мод даёт **иммунитет к урону от игроков**, пока ты у **своей** могилы после смерти.

## Как работает

1. Игрок умирает → 10 мин «окно могилы» (настраивается).
2. Подошёл к месту смерти (≤ 14 блоков) или к сущности своей могилы (≤ 10 блоков) → защита **30 сек**, продлевается, пока в зоне.
3. Вышел из зоны — ещё **60 сек** запаса (`lootGraceSeconds`), чтобы не убили при отходе.
4. В зоне лута по умолчанию **весь урон** блокируется (игроки, мобы, лава) — не только PvP.

Своя могила определяется по:

- тегам `graves.*` (датапack VT / ly-graves);
- нику на табличке могилы;
- UUID в тегах;
- близости к месту последней смерти.

## Команды (OP 4+)

| Команда | Описание |
|---------|----------|
| `/graveguard status` | Статус мода и (для OP) личная защита |
| `/graveguard reload` | Перечитать `config/isnix-graveguard.json` |

## Конфиг

`config/isnix-graveguard.json` — образец: [isnix-graveguard.json](config-samples/isnix-graveguard.json)

| Поле | По умолчанию | Смысл |
|------|--------------|--------|
| `protectionSeconds` | 30 | Длительность защиты (обновляется в зоне) |
| `eligibleMinutesAfterDeath` | 10 | Сколько минут после смерти действует логика |
| `nearGraveRadius` | 10.0 | Радиус поиска сущности могилы |
| `deathSiteRadius` | 14.0 | Радиус вокруг точки смерти (если могилу не видит мод) |
| `lootGraceSeconds` | 60 | Защита после выхода из зоны |
| `protectAllDamageInLootZone` | true | Полный иммунитет у могилы |
| `protectFromPlayersOnly` | true | Вне зоны лута — только PvP (если полный иммунитет выкл.) |
| `notifyPlayer` | true | Сообщение в action bar при активации |

## Установка

1. Собрать: `cd isnix-graveguard && ./gradlew build`
2. JAR в `mods/isnix-graveguard-1.0.2.jar`
3. **Restart** сервера (не `/reload` для модов)
4. Проверка: умри → подойди к могиле → `/graveguard status`

## Связь с правилами

По **A1.2** это починка техсистемы, не `/give`. Игрокам всё равно советуем не драться у могилы — защита не бесконечная.

## Версии

**1.0.2** — зона смерти 14 блоков, запас 60 сек после выхода, полный иммунитет у могилы, тик только для «после смерти».
**1.0.1** — исправлен StackOverflow в `isGraveEntity` (цикл пассажир ↔ транспорт у сущности могилы).
**1.0.0** — защита у VT Graves после смерти, конфиг, команды.
