name: Build ISNIX Reputation mod

on:
  push:
    branches: [main]
    paths:
      - 'isnix-reputation/**'
      - '.github/workflows/build-isnix-reputation.yml'
      - '.github/workflows/reusable-deploy-mod-sftp.yml'
  pull_request:
    branches: [main]
    paths:
      - 'isnix-reputation/**'
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '21'

      - name: Build mod
        working-directory: isnix-reputation
        run: chmod +x gradlew && ./gradlew build --no-daemon

      - name: Locate release JAR
        id: jar
        working-directory: isnix-reputation
        run: |
          JAR=$(ls build/libs/isnix-reputation-*.jar | grep -v sources | head -1)
          echo "path=build/libs/$(basename "$JAR")" >> "$GITHUB_OUTPUT"

      - uses: actions/upload-artifact@v4
        with:
          name: isnix-reputation-jar
          path: isnix-reputation/${{ steps.jar.outputs.path }}
          if-no-files-found: error

  deploy:
    needs: build
    if: github.ref == 'refs/heads/main' && github.event_name != 'pull_request'
    uses: ./.github/workflows/reusable-deploy-mod-sftp.yml
    secrets: inherit
    with:
      artifact-name: isnix-reputation-jar
