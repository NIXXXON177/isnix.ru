# Скопируйте в server-sftp.env и заполните SFTP_PASSWORD.
# Файл server-sftp.env в .gitignore — в git не попадёт.

SFTP_HOST=c11.play2go.cloud
SFTP_PORT=2022
SFTP_USER=60h38mtl1u.753fd3fb
SFTP_PASSWORD=
# Точка входа SFTP: обычно "." (корень уже = папка сервера). Не /home/container.
REMOTE_BASE=.
