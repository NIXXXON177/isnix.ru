name = "isnix-supabase-proxy"
main = "src/index.js"
compatibility_date = "2024-01-01"

# После деплоя: Dashboard → Worker → Settings → Domains & Routes → api.isnix.ru
# Или: wrangler deploy && настроить route в панели

[vars]
# SUPABASE_HOST = "yfrlgeztbaebdapdnefy.supabase.co"
